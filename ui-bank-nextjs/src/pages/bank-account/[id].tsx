import Layout from "../../components/Layout"
import Title from "../../components/utils/Title"

export default function BankAccountDashboard(props) {
    console.log(props);
    return (
        <Layout>
            <Title>Account Dashboard</Title>
        </Layout>
    );
}
